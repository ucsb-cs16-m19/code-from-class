// this is the definition of a function
// called main that returns an integer

// the main function is where your program starts
// the terminal calls main
int main() {

    // 0 is an exit code
    // it means that the program executed successfully

    /*
    
    this is a multi-line comment

    a non-zero exit code (always 1 in this class)
    means the program executed unsuccessfully
    (i.e., something went wrong)

    */

    return 0; // all this program does is return a number
}